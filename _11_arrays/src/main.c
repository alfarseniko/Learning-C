#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    /*int luckyNumbers[] = {4, 8, 15, 16}; // use curly braces to store data
    luckyNumbers[0] = 50000;
    printf("%d\n", luckyNumbers[0]);*/

    int numbers[100];
    numbers[0] = 1;
    numbers[2] = 1;
    numbers[4] = 1;
    numbers[6] = 1;

    return 0;
}