#include <stdio.h>
#include <stdlib.h>

int main()
{

    int age = 40;                         // use int for integers
    double gpa = 3.6;                     // use double for floating point numbers
    char grade = 'A';                     // use single quotes for characters
    char phrase[] = "This is a tutorial"; // use double quotes for strings

    return 0;
}